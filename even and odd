#include <stdio.h>

 main() 
{
    int a,b,c,m;
    printf("enter value of a : ");
    scanf("%d",&a);
   
    if(a%2==0)
    {
        printf("no is even");
        
    }
    else
    { 
        printf("no is odd");
    }
}
